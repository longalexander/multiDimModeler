import React from 'react';
import styled from 'styled-components';

const Container = styled.div`
  position: absolute;
  bottom: 0;
`;

function WorksheetFormulaView(){
  return(
    <Container>
      FormulaView
    </Container>
  )
}

export default WorksheetFormulaView;
